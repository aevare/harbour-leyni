Run `docker compose up -d` in this directory to start a local Vaultwarden instance.
The web vault is then available at http://localhost:8000.
Create a test account there and use its server URL when testing the app.
